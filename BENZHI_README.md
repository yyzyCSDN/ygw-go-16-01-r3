# RelayDock container

The image builds the deterministic relay demo. The application is an infrastructure component and keeps all state in process memory for reproducible tests; production deployments can replace the journal and sender adapters.
